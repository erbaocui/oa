<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.thinkgem.jeesite.modules.income.dao.ApplyPayContDao">
	<insert id="insert">
		INSERT INTO bm_apply_pay_cont(
			id,
			apply_pay_id,
			contract_id
		) VALUES (
			#{id},
			#{applyPayId},
			#{contractId}
		)
	</insert>
	

	
	<delete id="delete">
		DELETE FROM bm_apply_pay_cont
		WHERE
		     apply_pay_id = #{applyPayId}
		  and contract_id = #{contractId}
	</delete>
	
</mapper>